import React from 'react';
import { View, Image, Text } from 'react-native';

const Logo = () => (
	<View>
		<Image source={require('./images/Fan_Tipper_Logo_Green.png')}>
		</Image>
	</View>
	);

export default Logo;